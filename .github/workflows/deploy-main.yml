name: deploy-main

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "$EC2_SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          cat >>~/.ssh/config <<END
          Host Interview-Daily
            HostName $EC2_HOST
            User $EC2_USER
            IdentityFile ~/.ssh/id_rsa
            StrictHostKeyChecking no
          END
        env:
          EC2_USER: ubuntu
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}

      - name: Copy Workspace
        run: |
          ssh Interview-Daily 'sudo mkdir -p /opt/app'
          ssh Interview-Daily 'sudo chown ubuntu:ubuntu /opt/app'
          scp -r ./Backend/* Interview-Daily:/opt/app

      - name: Install dependencies
        run: |
          ssh Interview-Daily 'cd /opt/app && npm install'
          ssh Interview-Daily 'cd /opt/app && npx prisma generate'

      - name: Copy systemd service file
        run: |
          ssh Interview-Daily '
            echo "[Unit]
            Description=Interview Daily Backend
            After=network.target

            [Service]
            User=${USER}
            ExecStart=/usr/bin/npm run dev --prefix /opt/app/
            Restart=always

            [Install]
            WantedBy=multi-user.target" | sudo tee /etc/systemd/system/interview-daily.service
          '

      - name: Enable systemd service
        run: |
          ssh Interview-Daily 'sudo systemctl daemon-reload'
          ssh Interview-Daily 'sudo systemctl enable app'

      - name: Restart systemd service
        run: |
          ssh Interview-Daily 'sudo systemctl restart app'